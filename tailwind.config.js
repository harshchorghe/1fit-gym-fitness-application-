/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./app/**/*.{js,ts,jsx,tsx,mdx}",
    "./components/**/*.{js,ts,jsx,tsx,mdx}",
    // add more folders later if you create them (pages/, src/, etc.)
  ],
  theme: {
    extend: {
      // you can add custom colors, fonts, etc. here later
    },
  },
  plugins: [],
}